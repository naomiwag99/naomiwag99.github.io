var a=[
    "Aloof",
    "Arch",
    "Authority",
];

var b=[
    "Bear",
    "Belly",
    "Butterfly",
];

var c=[
    "Circumstance",
    "Complain",
    "Cow"
]

var randa=Math.floor((Math.random() * a.length) );
var randb=Math.floor((Math.random() * b.length) );
var randc=Math.floor((Math.random() * c.length) );

var acron=r[randa]+" "+b[randb]+" " + c[randc];
